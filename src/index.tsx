// Entry point - exact reproduction of current entry structure
// React import not needed for React 17+ JSX transform
import { registerRootComponent } from 'expo';
import App from './App';

// Register the main component
registerRootComponent(App);
